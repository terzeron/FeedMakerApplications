[project]
name = "fma"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
      "fm",
      "feedparser>=6.0.12",
      "termcolor>=3.1.0",
]

[tool.uv.sources]
fm = { path = "../fm", editable = true }
